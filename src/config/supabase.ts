/**
 * DEPRECATED — Supabase has been removed.
 * All data access now goes through src/services/api/.
 * This file is kept as an empty stub to avoid breaking
 * any lingering imports during the migration.
 */
export const supabase = null as never;
export type Database = Record<string, never>;
