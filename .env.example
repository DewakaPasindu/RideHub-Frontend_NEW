# Laravel Backend API
VITE_API_BASE_URL=http://localhost:8000/api

# Optional: WebSocket (Laravel Broadcasting / Pusher)
VITE_WS_URL=ws://localhost:6001
VITE_PUSHER_APP_KEY=
VITE_PUSHER_APP_CLUSTER=mt1

# Optional: Map provider (leave blank to use free OpenStreetMap/Nominatim fallback)
VITE_MAPBOX_TOKEN=
